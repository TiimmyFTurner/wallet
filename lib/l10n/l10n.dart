import 'package:flutter/material.dart';

class L10n {
  static const Locale en = Locale('en');
  static const Locale fa = Locale('fa');
  static const Locale system = Locale('system');
  static final List<Locale> all = <Locale>[en, fa];
}
